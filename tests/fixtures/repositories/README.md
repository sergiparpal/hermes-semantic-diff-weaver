# Generated repository fixtures

Evaluation and integration tests create isolated committed repositories from
`tests/fixtures/evaluation_cases.json`. Keeping the source revisions machine-readable avoids
platform-specific Git object fixtures while preserving deterministic old/new content and labels.
